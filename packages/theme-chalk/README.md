# @axis-ui/theme-chalk

[Axis-UI](https://github.com/CalWade/Axis-UI) 的默认主题（SCSS 源码 + 编译后 CSS）。

```ts
// 全量样式
import '@axis-ui/theme-chalk'

// 按组件引入
import '@axis-ui/theme-chalk/dist/button.css'
```

配合 `axis-ui/resolver` 使用时样式会自动按需注入。

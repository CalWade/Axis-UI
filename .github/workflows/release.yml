name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write # 允许创建 release 和提交代码
      pull-requests: write # 允许创建 PR (Version Packages)
      id-token: write # 必须：用于 OIDC Trusted Publisher

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org' # 必须：用于 OIDC 认证

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Packages
        run: pnpm build:all

      - name: Create Release Pull Request or Publish to npm
        id: changesets
        uses: changesets/action@v1
        with:
          # 当有新的 changeset 时，执行 version 命令更新版本
          version: pnpm run version
          # 当合并 Version Packages PR 时，执行 release 命令发布
          publish: pnpm run release
          commit: 'chore: update versions'
          title: 'chore: version packages'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Trusted Publisher (OIDC) 配置
          # NODE_AUTH_TOKEN 必须存在（触发 npm 的 OIDC 流程），但值为空或占位
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
